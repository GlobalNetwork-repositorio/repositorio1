export class Utilitarios {

    sumarColumdaData(data: any[], columnas:string[]): any {
        let rpt: any = [];
        columnas.map(x => {
            data.map(z => {
                
            })
        })
    }

}